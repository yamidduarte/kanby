import { GoogleSignin2Directive } from './google-signin-2.directive';

describe('GoogleSignin2Directive', () => {
  it('should create an instance', () => {
    const directive = new GoogleSignin2Directive();
    expect(directive).toBeTruthy();
  });
});
